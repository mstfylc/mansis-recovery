# Extracted Desktop Application

This directory contains the extracted Electron app bundle from the recovered Windows installer for review and continuity.

Excluded from Git:

- `node_modules/`
- installer binaries (`.exe`, `.dmg`, `.pkg`)
- raw `app.asar`

Recovered installer metadata is documented one level up in `desktop/README.md`.